/**
 * File: useAuth.ts
 *
 * Description: Custom React hook that manages user authentication state and operations.
 * Wraps the auth service and auth store to provide login, signup, logout, password reset,
 * and session persistence functionality to components throughout the app.
 *
 * Author: Navnit(Ninjacode911)
 */
